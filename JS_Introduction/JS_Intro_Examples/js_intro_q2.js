/*
Write a program that adds two numbers and display their sum using 
various methods in JavaScript.
*/

const num1 = 13;
const num2 = 15;

//add the 2 numbers
const sum = num1 + num2;

//display the sum
console.log(sum);

//store user input

/*The below 2 commands use promt which runs in the browser and not in 
node environment. Same code will work with the browser. 
const n1 = parseInt(prompt("Enter the first number: "));
const n2 = parseInt(prompt("Enter the second number: "));

//add the 2 numbers
const s = n1 + n2;

//display the sum
console.log(s);
*/


